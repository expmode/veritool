{
  "cache_key": "468d68243001a9cdbe13116df510e37b628124c1f8f3800c43548313718790cb",
  "code": "def read_local_context(base_dir: str, requested_path: str, file_map: dict[str, str]) -> str:\n    # Normalize base_dir to handle trailing slashes and relative components\n    has_leading_slash = base_dir.startswith('/')\n    base_dir_normalized = base_dir.replace('\\\\', '/')\n    base_parts = base_dir_normalized.split('/')\n    normalized_base = []\n    for part in base_parts:\n        if part == '' or part == '.':\n            continue\n        elif part == '..':\n            if normalized_base:\n                normalized_base.pop()\n        else:\n            normalized_base.append(part)\n    base_dir = '/'.join(normalized_base)\n    if has_leading_slash:\n        base_dir = '/' + base_dir\n\n    # Reject absolute paths in requested_path\n    if requested_path.startswith('/') or requested_path.startswith('\\\\'):\n        raise ValueError(\"Absolute paths are not allowed\")\n    if len(requested_path) > 1 and requested_path[1] == ':':\n        raise ValueError(\"Absolute paths are not allowed\")\n\n    # Normalize requested_path\n    requested_path_normalized = requested_path.replace('\\\\', '/')\n    parts = requested_path_normalized.split('/')\n    normalized_parts = []\n    for part in parts:\n        if part == '' or part == '.':\n            continue\n        elif part == '..':\n            if not normalized_parts:\n                raise ValueError(\"Path traversal detected\")\n            normalized_parts.pop()\n        else:\n            normalized_parts.append(part)\n    \n    normalized_requested = '/'.join(normalized_parts)\n\n    # Check extension\n    if not normalized_requested.endswith('.txt'):\n        raise ValueError(\"Only .txt files are allowed\")\n\n    # Construct full path\n    full_path = base_dir + '/' + normalized_requested\n\n    # Check containment within base_dir\n    if not full_path.startswith(base_dir):\n        raise ValueError(\"Path outside base directory\")\n\n    # Lookup in file_map\n    if full_path not in file_map:\n        raise ValueError(\"File not found in map\")\n\n    return file_map[full_path]",
  "generated_at": "2026-05-24T22:20:29+00:00",
  "iteration": 3,
  "prompt": "Write Python code for the function `read_local_context`.\nTool name: Local Context Reader\nSignature: read_local_context(base_dir: str, requested_path: str, file_map: dict[str, str]) -> str\nTask: Normalize a user-requested relative path, reject traversal or non-.txt access, and read the content from the provided in-memory file map.\nPrimary invariant: A request may only read `.txt` files that remain inside the declared safe base directory after normalization.\nConstraints:\n- Reject absolute paths and any normalized path outside the base directory.\n- Reject files whose normalized path does not end in .txt.\n- Do not touch the real filesystem; use only the provided file_map.\nCounterexamples from previous failed attempts:\n- forbidden_import: Candidates must stay within the provided runtime and cannot import modules.\n  Payload: {\"node_type\": \"Import\"}\n- syntax_error: The candidate could not be parsed as Python.\n  Payload: {\"line\": 60, \"message\": \"unindent does not match any outer indentation level\", \"offset\": 42}\nReturn only valid Python code with the requested function and any helper functions it needs.",
  "provider": "openai-compatible",
  "raw_response": "```python\ndef read_local_context(base_dir: str, requested_path: str, file_map: dict[str, str]) -> str:\n    # Normalize base_dir to handle trailing slashes and relative components\n    has_leading_slash = base_dir.startswith('/')\n    base_dir_normalized = base_dir.replace('\\\\', '/')\n    base_parts = base_dir_normalized.split('/')\n    normalized_base = []\n    for part in base_parts:\n        if part == '' or part == '.':\n            continue\n        elif part == '..':\n            if normalized_base:\n                normalized_base.pop()\n        else:\n            normalized_base.append(part)\n    base_dir = '/'.join(normalized_base)\n    if has_leading_slash:\n        base_dir = '/' + base_dir\n\n    # Reject absolute paths in requested_path\n    if requested_path.startswith('/') or requested_path.startswith('\\\\'):\n        raise ValueError(\"Absolute paths are not allowed\")\n    if len(requested_path) > 1 and requested_path[1] == ':':\n        raise ValueError(\"Absolute paths are not allowed\")\n\n    # Normalize requested_path\n    requested_path_normalized = requested_path.replace('\\\\', '/')\n    parts = requested_path_normalized.split('/')\n    normalized_parts = []\n    for part in parts:\n        if part == '' or part == '.':\n            continue\n        elif part == '..':\n            if not normalized_parts:\n                raise ValueError(\"Path traversal detected\")\n            normalized_parts.pop()\n        else:\n            normalized_parts.append(part)\n    \n    normalized_requested = '/'.join(normalized_parts)\n\n    # Check extension\n    if not normalized_requested.endswith('.txt'):\n        raise ValueError(\"Only .txt files are allowed\")\n\n    # Construct full path\n    full_path = base_dir + '/' + normalized_requested\n\n    # Check containment within base_dir\n    if not full_path.startswith(base_dir):\n        raise ValueError(\"Path outside base directory\")\n\n    # Lookup in file_map\n    if full_path not in file_map:\n        raise ValueError(\"File not found in map\")\n\n    return file_map[full_path]\n```"
}