{
  "cache_key": "8e7d94f00289a704ba0e2aa29bb1fbd0708c1c32e791d29eb3882115f7ea4fd5",
  "code": "def run_bounded_sql(query: str, rows: list[dict[str, int]]) -> list[dict[str, int]]:\n    # Define the allowed SQL keywords and operators\n    allowed_keywords = {'SELECT', 'FROM', 'WHERE', 'LIMIT'}\n    allowed_operators = {'=', '!=', '<', '<=', '>', '>='}\n    \n    # Split the query into tokens\n    tokens = query.split()\n    \n    # Check for forbidden keywords and semicolons\n    forbidden_keywords = {'DELETE', 'UPDATE', 'INSERT', 'DROP', 'ALTER', 'CREATE', 'PRAGMA', ';'}\n    if any(keyword in query.upper() for keyword in forbidden_keywords):\n        raise ValueError(\"Forbidden SQL operation detected.\")\n    \n    # Basic structure validation\n    if len(tokens) < 4 or tokens[0].upper() != 'SELECT' or 'FROM' not in tokens:\n        raise ValueError(\"Invalid SQL query structure.\")\n    \n    # Extract columns to select\n    select_index = tokens.index('SELECT') + 1\n    from_index = tokens.index('FROM')\n    columns = tokens[select_index:from_index]\n    if len(columns) == 1 and columns[0] == '*':\n        columns = list(rows[0].keys()) if rows else []\n    else:\n        columns = ''.join(columns).split(',')\n    \n    # Validate columns\n    for column in columns:\n        if column not in rows[0]:\n            raise ValueError(f\"Unknown column: {column}\")\n    \n    # Filter rows based on WHERE clause if present\n    filtered_rows = rows\n    if 'WHERE' in tokens:\n        where_index = tokens.index('WHERE') + 1\n        limit_index = tokens.index('LIMIT') if 'LIMIT' in tokens else len(tokens)\n        condition = tokens[where_index:limit_index]\n        \n        if len(condition) != 3 or condition[1] not in allowed_operators:\n            raise ValueError(\"Invalid WHERE clause.\")\n        \n        where_column, operator, value = condition\n        if where_column not in rows[0]:\n            raise ValueError(f\"Unknown column in WHERE clause: {where_column}\")\n        \n        try:\n            value = int(value)\n        except ValueError:\n            raise ValueError(\"WHERE clause value must be an integer.\")\n        \n        # Apply the WHERE condition\n        if operator == '=':\n            filtered_rows = [row for row in rows if row[where_column] == value]\n        elif operator == '!=':\n            filtered_rows = [row for row in rows if row[where_column] != value]\n        elif operator == '<':\n            filtered_rows = [row for row in rows if row[where_column] < value]\n        elif operator == '<=':\n            filtered_rows = [row for row in rows if row[where_column] <= value]\n        elif operator == '>':\n            filtered_rows = [row for row in rows if row[where_column] > value]\n        elif operator == '>=':\n            filtered_rows = [row for row in rows if row[where_column] >= value]\n    \n    # Apply LIMIT if present\n    if 'LIMIT' in tokens:\n        limit_index = tokens.index('LIMIT') + 1\n        try:\n            limit_value = int(tokens[limit_index])\n        except (IndexError, ValueError):\n            raise ValueError(\"Invalid LIMIT value.\")\n        filtered_rows = filtered_rows[:limit_value]\n    \n    # Select the specified columns\n    result = [{col: row[col] for col in columns} for row in filtered_rows]\n    \n    return result",
  "generated_at": "2026-05-24T20:36:32+00:00",
  "iteration": 3,
  "prompt": "Write Python code for the function `run_bounded_sql`.\nTool name: Bounded SQL Querier\nSignature: run_bounded_sql(query: str, rows: list[dict[str, int]]) -> list[dict[str, int]]\nTask: Execute a tiny SELECT-only SQL fragment over an in-memory table of rows.\nPrimary invariant: Only a safe SELECT grammar is allowed; mutating SQL or multi-statement queries must be rejected.\nConstraints:\n- Support only SELECT <columns> FROM rows [WHERE <column> <op> <int>] [LIMIT <int>].\n- Reject DELETE, UPDATE, INSERT, DROP, ALTER, CREATE, PRAGMA, semicolons, and unknown columns.\n- Treat the input `rows` as the only table.\nCounterexamples from previous failed attempts:\n- forbidden_import: Candidates must stay within the provided runtime and cannot import modules.\n  Payload: {\"node_type\": \"Import\"}\n- sql_exception: A safe query should execute cleanly in the bounded runner.\n  Payload: {\"error\": \"Unknown column: id,score\", \"query\": \"SELECT id,score FROM rows WHERE score >= 7\"}\nReturn only valid Python code with the requested function and any helper functions it needs.",
  "provider": "openai-compatible",
  "raw_response": "```python\ndef run_bounded_sql(query: str, rows: list[dict[str, int]]) -> list[dict[str, int]]:\n    # Define the allowed SQL keywords and operators\n    allowed_keywords = {'SELECT', 'FROM', 'WHERE', 'LIMIT'}\n    allowed_operators = {'=', '!=', '<', '<=', '>', '>='}\n    \n    # Split the query into tokens\n    tokens = query.split()\n    \n    # Check for forbidden keywords and semicolons\n    forbidden_keywords = {'DELETE', 'UPDATE', 'INSERT', 'DROP', 'ALTER', 'CREATE', 'PRAGMA', ';'}\n    if any(keyword in query.upper() for keyword in forbidden_keywords):\n        raise ValueError(\"Forbidden SQL operation detected.\")\n    \n    # Basic structure validation\n    if len(tokens) < 4 or tokens[0].upper() != 'SELECT' or 'FROM' not in tokens:\n        raise ValueError(\"Invalid SQL query structure.\")\n    \n    # Extract columns to select\n    select_index = tokens.index('SELECT') + 1\n    from_index = tokens.index('FROM')\n    columns = tokens[select_index:from_index]\n    if len(columns) == 1 and columns[0] == '*':\n        columns = list(rows[0].keys()) if rows else []\n    else:\n        columns = ''.join(columns).split(',')\n    \n    # Validate columns\n    for column in columns:\n        if column not in rows[0]:\n            raise ValueError(f\"Unknown column: {column}\")\n    \n    # Filter rows based on WHERE clause if present\n    filtered_rows = rows\n    if 'WHERE' in tokens:\n        where_index = tokens.index('WHERE') + 1\n        limit_index = tokens.index('LIMIT') if 'LIMIT' in tokens else len(tokens)\n        condition = tokens[where_index:limit_index]\n        \n        if len(condition) != 3 or condition[1] not in allowed_operators:\n            raise ValueError(\"Invalid WHERE clause.\")\n        \n        where_column, operator, value = condition\n        if where_column not in rows[0]:\n            raise ValueError(f\"Unknown column in WHERE clause: {where_column}\")\n        \n        try:\n            value = int(value)\n        except ValueError:\n            raise ValueError(\"WHERE clause value must be an integer.\")\n        \n        # Apply the WHERE condition\n        if operator == '=':\n            filtered_rows = [row for row in rows if row[where_column] == value]\n        elif operator == '!=':\n            filtered_rows = [row for row in rows if row[where_column] != value]\n        elif operator == '<':\n            filtered_rows = [row for row in rows if row[where_column] < value]\n        elif operator == '<=':\n            filtered_rows = [row for row in rows if row[where_column] <= value]\n        elif operator == '>':\n            filtered_rows = [row for row in rows if row[where_column] > value]\n        elif operator == '>=':\n            filtered_rows = [row for row in rows if row[where_column] >= value]\n    \n    # Apply LIMIT if present\n    if 'LIMIT' in tokens:\n        limit_index = tokens.index('LIMIT') + 1\n        try:\n            limit_value = int(tokens[limit_index])\n        except (IndexError, ValueError):\n            raise ValueError(\"Invalid LIMIT value.\")\n        filtered_rows = filtered_rows[:limit_value]\n    \n    # Select the specified columns\n    result = [{col: row[col] for col in columns} for row in filtered_rows]\n    \n    return result\n```"
}