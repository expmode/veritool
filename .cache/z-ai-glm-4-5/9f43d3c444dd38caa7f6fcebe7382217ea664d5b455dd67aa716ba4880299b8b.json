{
  "cache_key": "9f43d3c444dd38caa7f6fcebe7382217ea664d5b455dd67aa716ba4880299b8b",
  "code": "def read_local_context(base_dir: str, requested_path: str, file_map: dict[str, str]) -> str:\n    # Normalize the base directory\n    normalized_base = _normalize_path(base_dir)\n    \n    # Normalize the requested path\n    normalized_requested = _normalize_path(requested_path)\n    \n    # Check if the requested path is absolute\n    if normalized_requested.startswith('/'):\n        raise ValueError(\"Absolute paths are not allowed\")\n    \n    # Join the base directory and the requested path\n    if normalized_base and normalized_requested:\n        full_path = f\"{normalized_base}/{normalized_requested}\"\n    elif normalized_base:\n        full_path = normalized_base\n    else:\n        full_path = normalized_requested\n    \n    # Normalize the full path to resolve any '..' or '.'\n    normalized_full_path = _normalize_path(full_path)\n    \n    # Check if the normalized path is within the base directory\n    if normalized_base:\n        if normalized_full_path != normalized_base and not normalized_full_path.startswith(normalized_base + '/'):\n            raise ValueError(\"Path is outside the base directory\")\n    elif not normalized_full_path:\n        # If the base directory is empty and the normalized path is also empty,\n        # it means the requested path was something like '..' which resolved to nothing.\n        raise ValueError(\"Path is outside the base directory\")\n    \n    # Check if the path ends with .txt\n    if not normalized_full_path.endswith('.txt'):\n        raise ValueError(\"Only .txt files are allowed\")\n    \n    # Look up the file in the file_map\n    if normalized_full_path not in file_map:\n        raise FileNotFoundError(f\"File not found: {normalized_full_path}\")\n    \n    # Return the file content\n    return file_map[normalized_full_path]\n\ndef _normalize_path(path: str) -> str:\n    \"\"\"Normalize a path by removing redundant separators and resolving . and ..\"\"\"\n    if not path:\n        return ''\n    \n    # Split the path into components\n    components = path.split('/')\n    \n    # Process the components\n    normalized_components = []\n    for component in components:\n        if component == '' or component == '.':\n            continue\n        elif component == '..':\n            if normalized_components:\n                normalized_components.pop()\n        else:\n            normalized_components.append(component)\n    \n    # Join the components back into a path\n    normalized_path = '/'.join(normalized_components)\n    \n    return normalized_path",
  "generated_at": "2026-05-24T22:53:00+00:00",
  "iteration": 4,
  "prompt": "Write Python code for the function `read_local_context`.\nTool name: Local Context Reader\nSignature: read_local_context(base_dir: str, requested_path: str, file_map: dict[str, str]) -> str\nTask: Normalize a user-requested relative path, reject traversal or non-.txt access, and read the content from the provided in-memory file map.\nPrimary invariant: A request may only read `.txt` files that remain inside the declared safe base directory after normalization.\nConstraints:\n- Reject absolute paths and any normalized path outside the base directory.\n- Reject files whose normalized path does not end in .txt.\n- Do not touch the real filesystem; use only the provided file_map.\nCounterexamples from previous failed attempts:\n- forbidden_import: Candidates must stay within the provided runtime and cannot import modules.\n  Payload: {\"node_type\": \"Import\"}\n- forbidden_import: Candidates must stay within the provided runtime and cannot import modules.\n  Payload: {\"node_type\": \"Import\"}\n- forbidden_capability: Candidate referenced a module or capability that is outside the safe micro-tool model.\n  Payload: {\"term\": \"os.\"}\nReturn only valid Python code with the requested function and any helper functions it needs.",
  "provider": "openai-compatible",
  "raw_response": "```python\ndef read_local_context(base_dir: str, requested_path: str, file_map: dict[str, str]) -> str:\n    # Normalize the base directory\n    normalized_base = _normalize_path(base_dir)\n    \n    # Normalize the requested path\n    normalized_requested = _normalize_path(requested_path)\n    \n    # Check if the requested path is absolute\n    if normalized_requested.startswith('/'):\n        raise ValueError(\"Absolute paths are not allowed\")\n    \n    # Join the base directory and the requested path\n    if normalized_base and normalized_requested:\n        full_path = f\"{normalized_base}/{normalized_requested}\"\n    elif normalized_base:\n        full_path = normalized_base\n    else:\n        full_path = normalized_requested\n    \n    # Normalize the full path to resolve any '..' or '.'\n    normalized_full_path = _normalize_path(full_path)\n    \n    # Check if the normalized path is within the base directory\n    if normalized_base:\n        if normalized_full_path != normalized_base and not normalized_full_path.startswith(normalized_base + '/'):\n            raise ValueError(\"Path is outside the base directory\")\n    elif not normalized_full_path:\n        # If the base directory is empty and the normalized path is also empty,\n        # it means the requested path was something like '..' which resolved to nothing.\n        raise ValueError(\"Path is outside the base directory\")\n    \n    # Check if the path ends with .txt\n    if not normalized_full_path.endswith('.txt'):\n        raise ValueError(\"Only .txt files are allowed\")\n    \n    # Look up the file in the file_map\n    if normalized_full_path not in file_map:\n        raise FileNotFoundError(f\"File not found: {normalized_full_path}\")\n    \n    # Return the file content\n    return file_map[normalized_full_path]\n\ndef _normalize_path(path: str) -> str:\n    \"\"\"Normalize a path by removing redundant separators and resolving . and ..\"\"\"\n    if not path:\n        return ''\n    \n    # Split the path into components\n    components = path.split('/')\n    \n    # Process the components\n    normalized_components = []\n    for component in components:\n        if component == '' or component == '.':\n            continue\n        elif component == '..':\n            if normalized_components:\n                normalized_components.pop()\n        else:\n            normalized_components.append(component)\n    \n    # Join the components back into a path\n    normalized_path = '/'.join(normalized_components)\n    \n    return normalized_path\n```"
}