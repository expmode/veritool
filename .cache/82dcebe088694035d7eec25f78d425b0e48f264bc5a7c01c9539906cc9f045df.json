{
  "cache_key": "82dcebe088694035d7eec25f78d425b0e48f264bc5a7c01c9539906cc9f045df",
  "code": "import ast\n\nclass BoundedEvaluator(ast.NodeVisitor):\n    def __init__(self, variables, max_nodes):\n        self.variables = variables\n        self.max_nodes = max_nodes\n        self.node_count = 0\n\n    def visit(self, node):\n        if self.node_count >= self.max_nodes:\n            raise ValueError(\"Expression exceeds maximum allowed nodes.\")\n        self.node_count += 1\n        return super().visit(node)\n\n    def visit_Expr(self, node):\n        return self.visit(node.value)\n\n    def visit_BinOp(self, node):\n        left = self.visit(node.left)\n        right = self.visit(node.right)\n        if isinstance(node.op, ast.Add):\n            return left + right\n        elif isinstance(node.op, ast.Sub):\n            return left - right\n        elif isinstance(node.op, ast.Mult):\n            return left * right\n        elif isinstance(node.op, ast.FloorDiv):\n            if right == 0:\n                raise ZeroDivisionError(\"Division by zero.\")\n            return left // right\n        else:\n            raise ValueError(\"Unsupported operation.\")\n\n    def visit_UnaryOp(self, node):\n        if isinstance(node.op, ast.USub):\n            return -self.visit(node.operand)\n        else:\n            raise ValueError(\"Unsupported unary operation.\")\n\n    def visit_Num(self, node):\n        return node.n\n\n    def visit_Name(self, node):\n        if node.id in self.variables:\n            return self.variables[node.id]\n        else:\n            raise ValueError(f\"Unknown variable: {node.id}\")\n\n    def visit_Paren(self, node):\n        return self.visit(node.value)\n\n    def visit_Module(self, node):\n        if len(node.body) != 1:\n            raise ValueError(\"Only single expressions are allowed.\")\n        return self.visit(node.body[0])\n\n    def generic_visit(self, node):\n        raise ValueError(\"Unsupported expression.\")\n\ndef bounded_evaluate(expression: str, variables: dict[str, int], max_nodes: int = 32) -> int:\n    try:\n        tree = ast.parse(expression, mode='eval')\n        evaluator = BoundedEvaluator(variables, max_nodes)\n        return evaluator.visit(tree)\n    except Exception as e:\n        raise ValueError(f\"Invalid expression: {e}\")",
  "generated_at": "2026-05-24T20:37:06+00:00",
  "iteration": 1,
  "prompt": "Write Python code for the function `bounded_evaluate`.\nTool name: Compute-Bounded Evaluator\nSignature: bounded_evaluate(expression: str, variables: dict[str, int], max_nodes: int = 32) -> int\nTask: Evaluate a tiny arithmetic expression language with explicit resource bounds.\nPrimary invariant: Only a bounded whitelist of arithmetic AST nodes is allowed, and evaluation must reject unsupported or oversized expressions.\nConstraints:\n- Support integers, variables, parentheses, unary minus, +, -, *, and //.\n- Reject function calls, attributes, exponentiation, unknown variables, and expressions with more than max_nodes AST nodes.\n- Return the same result as the reference interpreter on bounded inputs.\nReturn only valid Python code with the requested function and any helper functions it needs.",
  "provider": "openai-compatible",
  "raw_response": "```python\nimport ast\n\nclass BoundedEvaluator(ast.NodeVisitor):\n    def __init__(self, variables, max_nodes):\n        self.variables = variables\n        self.max_nodes = max_nodes\n        self.node_count = 0\n\n    def visit(self, node):\n        if self.node_count >= self.max_nodes:\n            raise ValueError(\"Expression exceeds maximum allowed nodes.\")\n        self.node_count += 1\n        return super().visit(node)\n\n    def visit_Expr(self, node):\n        return self.visit(node.value)\n\n    def visit_BinOp(self, node):\n        left = self.visit(node.left)\n        right = self.visit(node.right)\n        if isinstance(node.op, ast.Add):\n            return left + right\n        elif isinstance(node.op, ast.Sub):\n            return left - right\n        elif isinstance(node.op, ast.Mult):\n            return left * right\n        elif isinstance(node.op, ast.FloorDiv):\n            if right == 0:\n                raise ZeroDivisionError(\"Division by zero.\")\n            return left // right\n        else:\n            raise ValueError(\"Unsupported operation.\")\n\n    def visit_UnaryOp(self, node):\n        if isinstance(node.op, ast.USub):\n            return -self.visit(node.operand)\n        else:\n            raise ValueError(\"Unsupported unary operation.\")\n\n    def visit_Num(self, node):\n        return node.n\n\n    def visit_Name(self, node):\n        if node.id in self.variables:\n            return self.variables[node.id]\n        else:\n            raise ValueError(f\"Unknown variable: {node.id}\")\n\n    def visit_Paren(self, node):\n        return self.visit(node.value)\n\n    def visit_Module(self, node):\n        if len(node.body) != 1:\n            raise ValueError(\"Only single expressions are allowed.\")\n        return self.visit(node.body[0])\n\n    def generic_visit(self, node):\n        raise ValueError(\"Unsupported expression.\")\n\ndef bounded_evaluate(expression: str, variables: dict[str, int], max_nodes: int = 32) -> int:\n    try:\n        tree = ast.parse(expression, mode='eval')\n        evaluator = BoundedEvaluator(variables, max_nodes)\n        return evaluator.visit(tree)\n    except Exception as e:\n        raise ValueError(f\"Invalid expression: {e}\")\n```\n\nThis code defines a `bounded_evaluate` function that evaluates arithmetic expressions with constraints on the number of AST nodes and the types of operations allowed. It uses Python's `ast` module to parse and traverse the expression, ensuring that only supported operations and variables are used, and that the expression does not exceed the specified node count."
}