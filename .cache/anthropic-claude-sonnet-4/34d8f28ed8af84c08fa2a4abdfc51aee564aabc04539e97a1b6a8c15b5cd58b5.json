{
  "cache_key": "34d8f28ed8af84c08fa2a4abdfc51aee564aabc04539e97a1b6a8c15b5cd58b5",
  "code": "def read_local_context(base_dir: str, requested_path: str, file_map: dict[str, str]) -> str:\n    # Reject absolute paths\n    if requested_path.startswith('/') or (len(requested_path) > 1 and requested_path[1] == ':'):\n        raise ValueError(\"Absolute paths are not allowed\")\n    \n    # Normalize the path by resolving .. and . components\n    def normalize_path(path):\n        parts = []\n        for part in path.split('/'):\n            if part == '' or part == '.':\n                continue\n            elif part == '..':\n                if parts:\n                    parts.pop()\n            else:\n                parts.append(part)\n        return '/'.join(parts)\n    \n    # Normalize both base_dir and requested_path\n    normalized_base = normalize_path(base_dir)\n    normalized_requested = normalize_path(requested_path)\n    \n    # Construct the full normalized path\n    if normalized_base:\n        full_path = normalized_base + '/' + normalized_requested\n    else:\n        full_path = normalized_requested\n    \n    # Normalize the full path to handle any remaining .. components\n    full_normalized = normalize_path(full_path)\n    \n    # Check if the normalized path is still within the base directory\n    if normalized_base:\n        if not (full_normalized == normalized_base or full_normalized.startswith(normalized_base + '/')):\n            raise ValueError(\"Path traversal outside base directory is not allowed\")\n    \n    # Check if the file ends with .txt\n    if not full_normalized.endswith('.txt'):\n        raise ValueError(\"Only .txt files are allowed\")\n    \n    # Read from the file map\n    if full_normalized not in file_map:\n        raise FileNotFoundError(f\"File not found: {full_normalized}\")\n    \n    return file_map[full_normalized]",
  "generated_at": "2026-05-24T21:49:39+00:00",
  "iteration": 2,
  "prompt": "Write Python code for the function `read_local_context`.\nTool name: Local Context Reader\nSignature: read_local_context(base_dir: str, requested_path: str, file_map: dict[str, str]) -> str\nTask: Normalize a user-requested relative path, reject traversal or non-.txt access, and read the content from the provided in-memory file map.\nPrimary invariant: A request may only read `.txt` files that remain inside the declared safe base directory after normalization.\nConstraints:\n- Reject absolute paths and any normalized path outside the base directory.\n- Reject files whose normalized path does not end in .txt.\n- Do not touch the real filesystem; use only the provided file_map.\nCounterexamples from previous failed attempts:\n- forbidden_import: Candidates must stay within the provided runtime and cannot import modules.\n  Payload: {\"node_type\": \"Import\"}\nReturn only valid Python code with the requested function and any helper functions it needs.",
  "provider": "openai-compatible",
  "raw_response": "```python\ndef read_local_context(base_dir: str, requested_path: str, file_map: dict[str, str]) -> str:\n    # Reject absolute paths\n    if requested_path.startswith('/') or (len(requested_path) > 1 and requested_path[1] == ':'):\n        raise ValueError(\"Absolute paths are not allowed\")\n    \n    # Normalize the path by resolving .. and . components\n    def normalize_path(path):\n        parts = []\n        for part in path.split('/'):\n            if part == '' or part == '.':\n                continue\n            elif part == '..':\n                if parts:\n                    parts.pop()\n            else:\n                parts.append(part)\n        return '/'.join(parts)\n    \n    # Normalize both base_dir and requested_path\n    normalized_base = normalize_path(base_dir)\n    normalized_requested = normalize_path(requested_path)\n    \n    # Construct the full normalized path\n    if normalized_base:\n        full_path = normalized_base + '/' + normalized_requested\n    else:\n        full_path = normalized_requested\n    \n    # Normalize the full path to handle any remaining .. components\n    full_normalized = normalize_path(full_path)\n    \n    # Check if the normalized path is still within the base directory\n    if normalized_base:\n        if not (full_normalized == normalized_base or full_normalized.startswith(normalized_base + '/')):\n            raise ValueError(\"Path traversal outside base directory is not allowed\")\n    \n    # Check if the file ends with .txt\n    if not full_normalized.endswith('.txt'):\n        raise ValueError(\"Only .txt files are allowed\")\n    \n    # Read from the file map\n    if full_normalized not in file_map:\n        raise FileNotFoundError(f\"File not found: {full_normalized}\")\n    \n    return file_map[full_normalized]\n```"
}