{
  "cache_key": "361be02b79240f3d85bf4bb533759fbec8fadbbfa24778099381b844f96e5415",
  "code": "import re\nfrom typing import List, Dict, Any, Optional\n\ndef run_bounded_sql(query: str, rows: list[dict[str, int]]) -> list[dict[str, int]]:\n    \"\"\"Execute a safe SELECT-only SQL query on in-memory data.\"\"\"\n    \n    # Normalize query\n    query = query.strip()\n    \n    # Security checks\n    if not _is_safe_query(query):\n        raise ValueError(\"Unsafe SQL query detected\")\n    \n    # Parse the query\n    parsed = _parse_select_query(query)\n    \n    # Get available columns from rows\n    available_columns = set()\n    if rows:\n        available_columns = set(rows[0].keys())\n    \n    # Validate columns exist\n    _validate_columns(parsed, available_columns)\n    \n    # Execute the query\n    result = rows[:]\n    \n    # Apply WHERE clause\n    if parsed['where']:\n        result = _apply_where_filter(result, parsed['where'])\n    \n    # Apply column selection\n    if parsed['columns'] != ['*']:\n        result = _select_columns(result, parsed['columns'])\n    \n    # Apply LIMIT\n    if parsed['limit'] is not None:\n        result = result[:parsed['limit']]\n    \n    return result\n\ndef _is_safe_query(query: str) -> bool:\n    \"\"\"Check if query contains only safe SELECT operations.\"\"\"\n    query_upper = query.upper()\n    \n    # Check for forbidden keywords\n    forbidden = ['DELETE', 'UPDATE', 'INSERT', 'DROP', 'ALTER', 'CREATE', 'PRAGMA']\n    for keyword in forbidden:\n        if keyword in query_upper:\n            return False\n    \n    # Check for semicolons (no multi-statement)\n    if ';' in query:\n        return False\n    \n    # Must start with SELECT\n    if not query_upper.strip().startswith('SELECT'):\n        return False\n    \n    return True\n\ndef _parse_select_query(query: str) -> dict:\n    \"\"\"Parse a SELECT query into components.\"\"\"\n    query = query.strip()\n    \n    # Use regex to parse the query structure\n    pattern = r'SELECT\\s+(.*?)\\s+FROM\\s+rows(?:\\s+WHERE\\s+(.*?))?(?:\\s+LIMIT\\s+(\\d+))?$'\n    match = re.match(pattern, query, re.IGNORECASE | re.DOTALL)\n    \n    if not match:\n        raise ValueError(\"Invalid SELECT query format\")\n    \n    columns_str, where_str, limit_str = match.groups()\n    \n    # Parse columns\n    if columns_str.strip() == '*':\n        columns = ['*']\n    else:\n        columns = [col.strip() for col in columns_str.split(',')]\n    \n    # Parse WHERE clause\n    where_clause = None\n    if where_str:\n        where_clause = _parse_where_clause(where_str.strip())\n    \n    # Parse LIMIT\n    limit = None\n    if limit_str:\n        limit = int(limit_str)\n    \n    return {\n        'columns': columns,\n        'where': where_clause,\n        'limit': limit\n    }\n\ndef _parse_where_clause(where_str: str) -> dict:\n    \"\"\"Parse WHERE clause into column, operator, and value.\"\"\"\n    # Support operators: =, !=, <, >, <=, >=\n    pattern = r'(\\w+)\\s*(=|!=|<=|>=|<|>)\\s*(-?\\d+)'\n    match = re.match(pattern, where_str)\n    \n    if not match:\n        raise ValueError(\"Invalid WHERE clause format\")\n    \n    column, operator, value = match.groups()\n    \n    return {\n        'column': column,\n        'operator': operator,\n        'value': int(value)\n    }\n\ndef _validate_columns(parsed: dict, available_columns: set) -> None:\n    \"\"\"Validate that all referenced columns exist.\"\"\"\n    columns_to_check = set()\n    \n    # Check SELECT columns\n    if parsed['columns'] != ['*']:\n        columns_to_check.update(parsed['columns'])\n    \n    # Check WHERE column\n    if parsed['where']:\n        columns_to_check.add(parsed['where']['column'])\n    \n    # Validate all columns exist\n    for column in columns_to_check:\n        if column not in available_columns:\n            raise ValueError(f\"Unknown column: {column}\")\n\ndef _apply_where_filter(rows: list[dict[str, int]], where_clause: dict) -> list[dict[str, int]]:\n    \"\"\"Apply WHERE clause filtering.\"\"\"\n    column = where_clause['column']\n    operator = where_clause['operator']\n    value = where_clause['value']\n    \n    filtered_rows = []\n    \n    for row in rows:\n        row_value = row.get(column)\n        if row_value is None:\n            continue\n        \n        if _evaluate_condition(row_value, operator, value):\n            filtered_rows.append(row)\n    \n    return filtered_rows\n\ndef _evaluate_condition(row_value: int, operator: str, target_value: int) -> bool:\n    \"\"\"Evaluate a single WHERE condition.\"\"\"\n    if operator == '=':\n        return row_value == target_value\n    elif operator == '!=':\n        return row_value != target_value\n    elif operator == '<':\n        return row_value < target_value\n    elif operator == '>':\n        return row_value > target_value\n    elif operator == '<=':\n        return row_value <= target_value\n    elif operator == '>=':\n        return row_value >= target_value\n    else:\n        raise ValueError(f\"Unsupported operator: {operator}\")\n\ndef _select_columns(rows: list[dict[str, int]], columns: list[str]) -> list[dict[str, int]]:\n    \"\"\"Select only specified columns from rows.\"\"\"\n    selected_rows = []\n    \n    for row in rows:\n        selected_row = {}\n        for column in columns:\n            if column in row:\n                selected_row[column] = row[column]\n        selected_rows.append(selected_row)\n    \n    return selected_rows",
  "generated_at": "2026-05-24T21:49:55+00:00",
  "iteration": 1,
  "prompt": "Write Python code for the function `run_bounded_sql`.\nTool name: Bounded SQL Querier\nSignature: run_bounded_sql(query: str, rows: list[dict[str, int]]) -> list[dict[str, int]]\nTask: Execute a tiny SELECT-only SQL fragment over an in-memory table of rows.\nPrimary invariant: Only a safe SELECT grammar is allowed; mutating SQL or multi-statement queries must be rejected.\nConstraints:\n- Support only SELECT <columns> FROM rows [WHERE <column> <op> <int>] [LIMIT <int>].\n- Reject DELETE, UPDATE, INSERT, DROP, ALTER, CREATE, PRAGMA, semicolons, and unknown columns.\n- Treat the input `rows` as the only table.\nReturn only valid Python code with the requested function and any helper functions it needs.",
  "provider": "openai-compatible",
  "raw_response": "```python\nimport re\nfrom typing import List, Dict, Any, Optional\n\ndef run_bounded_sql(query: str, rows: list[dict[str, int]]) -> list[dict[str, int]]:\n    \"\"\"Execute a safe SELECT-only SQL query on in-memory data.\"\"\"\n    \n    # Normalize query\n    query = query.strip()\n    \n    # Security checks\n    if not _is_safe_query(query):\n        raise ValueError(\"Unsafe SQL query detected\")\n    \n    # Parse the query\n    parsed = _parse_select_query(query)\n    \n    # Get available columns from rows\n    available_columns = set()\n    if rows:\n        available_columns = set(rows[0].keys())\n    \n    # Validate columns exist\n    _validate_columns(parsed, available_columns)\n    \n    # Execute the query\n    result = rows[:]\n    \n    # Apply WHERE clause\n    if parsed['where']:\n        result = _apply_where_filter(result, parsed['where'])\n    \n    # Apply column selection\n    if parsed['columns'] != ['*']:\n        result = _select_columns(result, parsed['columns'])\n    \n    # Apply LIMIT\n    if parsed['limit'] is not None:\n        result = result[:parsed['limit']]\n    \n    return result\n\ndef _is_safe_query(query: str) -> bool:\n    \"\"\"Check if query contains only safe SELECT operations.\"\"\"\n    query_upper = query.upper()\n    \n    # Check for forbidden keywords\n    forbidden = ['DELETE', 'UPDATE', 'INSERT', 'DROP', 'ALTER', 'CREATE', 'PRAGMA']\n    for keyword in forbidden:\n        if keyword in query_upper:\n            return False\n    \n    # Check for semicolons (no multi-statement)\n    if ';' in query:\n        return False\n    \n    # Must start with SELECT\n    if not query_upper.strip().startswith('SELECT'):\n        return False\n    \n    return True\n\ndef _parse_select_query(query: str) -> dict:\n    \"\"\"Parse a SELECT query into components.\"\"\"\n    query = query.strip()\n    \n    # Use regex to parse the query structure\n    pattern = r'SELECT\\s+(.*?)\\s+FROM\\s+rows(?:\\s+WHERE\\s+(.*?))?(?:\\s+LIMIT\\s+(\\d+))?$'\n    match = re.match(pattern, query, re.IGNORECASE | re.DOTALL)\n    \n    if not match:\n        raise ValueError(\"Invalid SELECT query format\")\n    \n    columns_str, where_str, limit_str = match.groups()\n    \n    # Parse columns\n    if columns_str.strip() == '*':\n        columns = ['*']\n    else:\n        columns = [col.strip() for col in columns_str.split(',')]\n    \n    # Parse WHERE clause\n    where_clause = None\n    if where_str:\n        where_clause = _parse_where_clause(where_str.strip())\n    \n    # Parse LIMIT\n    limit = None\n    if limit_str:\n        limit = int(limit_str)\n    \n    return {\n        'columns': columns,\n        'where': where_clause,\n        'limit': limit\n    }\n\ndef _parse_where_clause(where_str: str) -> dict:\n    \"\"\"Parse WHERE clause into column, operator, and value.\"\"\"\n    # Support operators: =, !=, <, >, <=, >=\n    pattern = r'(\\w+)\\s*(=|!=|<=|>=|<|>)\\s*(-?\\d+)'\n    match = re.match(pattern, where_str)\n    \n    if not match:\n        raise ValueError(\"Invalid WHERE clause format\")\n    \n    column, operator, value = match.groups()\n    \n    return {\n        'column': column,\n        'operator': operator,\n        'value': int(value)\n    }\n\ndef _validate_columns(parsed: dict, available_columns: set) -> None:\n    \"\"\"Validate that all referenced columns exist.\"\"\"\n    columns_to_check = set()\n    \n    # Check SELECT columns\n    if parsed['columns'] != ['*']:\n        columns_to_check.update(parsed['columns'])\n    \n    # Check WHERE column\n    if parsed['where']:\n        columns_to_check.add(parsed['where']['column'])\n    \n    # Validate all columns exist\n    for column in columns_to_check:\n        if column not in available_columns:\n            raise ValueError(f\"Unknown column: {column}\")\n\ndef _apply_where_filter(rows: list[dict[str, int]], where_clause: dict) -> list[dict[str, int]]:\n    \"\"\"Apply WHERE clause filtering.\"\"\"\n    column = where_clause['column']\n    operator = where_clause['operator']\n    value = where_clause['value']\n    \n    filtered_rows = []\n    \n    for row in rows:\n        row_value = row.get(column)\n        if row_value is None:\n            continue\n        \n        if _evaluate_condition(row_value, operator, value):\n            filtered_rows.append(row)\n    \n    return filtered_rows\n\ndef _evaluate_condition(row_value: int, operator: str, target_value: int) -> bool:\n    \"\"\"Evaluate a single WHERE condition.\"\"\"\n    if operator == '=':\n        return row_value == target_value\n    elif operator == '!=':\n        return row_value != target_value\n    elif operator == '<':\n        return row_value < target_value\n    elif operator == '>':\n        return row_value > target_value\n    elif operator == '<=':\n        return row_value <= target_value\n    elif operator == '>=':\n        return row_value >= target_value\n    else:\n        raise ValueError(f\"Unsupported operator: {operator}\")\n\ndef _select_columns(rows: list[dict[str, int]], columns: list[str]) -> list[dict[str, int]]:\n    \"\"\"Select only specified columns from rows.\"\"\"\n    selected_rows = []\n    \n    for row in rows:\n        selected_row = {}\n        for column in columns:\n            if column in row:\n                selected_row[column] = row[column]\n        selected_rows.append(selected_row)\n    \n    return selected_rows\n```"
}