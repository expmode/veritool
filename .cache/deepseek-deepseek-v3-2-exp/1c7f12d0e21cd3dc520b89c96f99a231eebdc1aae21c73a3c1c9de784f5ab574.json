{
  "cache_key": "1c7f12d0e21cd3dc520b89c96f99a231eebdc1aae21c73a3c1c9de784f5ab574",
  "code": "def bounded_evaluate(expression: str, variables: dict[str, int], max_nodes: int = 32) -> int:\n    # Helper function to count nodes in AST\n    def count_nodes(node):\n        if isinstance(node, tuple):\n            node_type = node[0]\n            if node_type in ('num', 'var'):\n                return 1\n            elif node_type == 'unary':\n                return 1 + count_nodes(node[1])\n            elif node_type in ('add', 'sub', 'mul', 'floordiv'):\n                return 1 + count_nodes(node[1]) + count_nodes(node[2])\n        return 0\n    \n    # Helper function to evaluate AST\n    def eval_node(node):\n        node_type = node[0]\n        \n        if node_type == 'num':\n            return node[1]\n        elif node_type == 'var':\n            var_name = node[1]\n            if var_name not in variables:\n                raise ValueError(f\"Unknown variable: {var_name}\")\n            return variables[var_name]\n        elif node_type == 'unary':\n            return -eval_node(node[1])\n        elif node_type == 'add':\n            return eval_node(node[1]) + eval_node(node[2])\n        elif node_type == 'sub':\n            return eval_node(node[1]) - eval_node(node[2])\n        elif node_type == 'mul':\n            return eval_node(node[1]) * eval_node(node[2])\n        elif node_type == 'floordiv':\n            divisor = eval_node(node[2])\n            if divisor == 0:\n                raise ZeroDivisionError(\"Integer division by zero\")\n            return eval_node(node[1]) // divisor\n    \n    # Simple recursive descent parser\n    def parse_expression(tokens):\n        return parse_add_sub(tokens)\n    \n    def parse_add_sub(tokens):\n        left = parse_mul_div(tokens)\n        \n        while tokens and tokens[0] in ('+', '-'):\n            op = tokens.pop(0)\n            right = parse_mul_div(tokens)\n            if op == '+':\n                left = ('add', left, right)\n            else:\n                left = ('sub', left, right)\n        \n        return left\n    \n    def parse_mul_div(tokens):\n        left = parse_unary(tokens)\n        \n        while tokens and tokens[0] in ('*', '//'):\n            op = tokens.pop(0)\n            right = parse_unary(tokens)\n            if op == '*':\n                left = ('mul', left, right)\n            else:\n                left = ('floordiv', left, right)\n        \n        return left\n    \n    def parse_unary(tokens):\n        if tokens and tokens[0] == '-':\n            tokens.pop(0)\n            expr = parse_unary(tokens)\n            return ('unary', expr)\n        return parse_primary(tokens)\n    \n    def parse_primary(tokens):\n        if not tokens:\n            raise ValueError(\"Unexpected end of expression\")\n        \n        token = tokens.pop(0)\n        \n        if token == '(':\n            expr = parse_expression(tokens)\n            if not tokens or tokens.pop(0) != ')':\n                raise ValueError(\"Missing closing parenthesis\")\n            return expr\n        elif token.isdigit() or (token[0] == '-' and token[1:].isdigit()):\n            return ('num', int(token))\n        elif token.isidentifier():\n            return ('var', token)\n        else:\n            raise ValueError(f\"Unexpected token: {token}\")\n    \n    # Tokenize the expression\n    def tokenize(expr):\n        tokens = []\n        i = 0\n        n = len(expr)\n        \n        while i < n:\n            ch = expr[i]\n            \n            if ch.isspace():\n                i += 1\n            elif ch.isdigit():\n                j = i\n                while j < n and expr[j].isdigit():\n                    j += 1\n                tokens.append(expr[i:j])\n                i = j\n            elif ch.isalpha() or ch == '_':\n                j = i\n                while j < n and (expr[j].isalnum() or expr[j] == '_'):\n                    j += 1\n                tokens.append(expr[i:j])\n                i = j\n            elif ch in '+-*()':\n                # Handle negative numbers\n                if ch == '-' and (not tokens or tokens[-1] in '+-*//('):\n                    j = i + 1\n                    while j < n and expr[j].isdigit():\n                        j += 1\n                    if j > i + 1:  # We found digits after '-'\n                        tokens.append(expr[i:j])\n                        i = j\n                        continue\n                tokens.append(ch)\n                i += 1\n            elif ch == '/':\n                if i + 1 < n and expr[i + 1] == '/':\n                    tokens.append('//')\n                    i += 2\n                else:\n                    raise ValueError(\"Single '/' not allowed, use '//' for integer division\")\n            else:\n                raise ValueError(f\"Invalid character: {ch}\")\n        \n        return tokens\n    \n    try:\n        # Tokenize and parse\n        tokens = tokenize(expression)\n        ast = parse_expression(tokens)\n        \n        # Check for extra tokens\n        if tokens:\n            raise ValueError(f\"Unexpected tokens at end: {tokens}\")\n        \n        # Count nodes and check bounds\n        node_count = count_nodes(ast)\n        if node_count > max_nodes:\n            raise ValueError(f\"Expression exceeds maximum node count: {node_count} > {max_nodes}\")\n        \n        # Evaluate\n        return eval_node(ast)\n        \n    except (ValueError, ZeroDivisionError) as e:\n        # Re-raise with appropriate error type\n        raise e",
  "generated_at": "2026-05-24T22:21:06+00:00",
  "iteration": 2,
  "prompt": "Write Python code for the function `bounded_evaluate`.\nTool name: Compute-Bounded Evaluator\nSignature: bounded_evaluate(expression: str, variables: dict[str, int], max_nodes: int = 32) -> int\nTask: Evaluate a tiny arithmetic expression language with explicit resource bounds.\nPrimary invariant: Only a bounded whitelist of arithmetic AST nodes is allowed, and evaluation must reject unsupported or oversized expressions.\nConstraints:\n- Support integers, variables, parentheses, unary minus, +, -, *, and //.\n- Reject function calls, attributes, exponentiation, unknown variables, and expressions with more than max_nodes AST nodes.\n- Return the same result as the reference interpreter on bounded inputs.\nCounterexamples from previous failed attempts:\n- forbidden_import: Candidates must stay within the provided runtime and cannot import modules.\n  Payload: {\"node_type\": \"Import\"}\nReturn only valid Python code with the requested function and any helper functions it needs.",
  "provider": "openai-compatible",
  "raw_response": "```python\ndef bounded_evaluate(expression: str, variables: dict[str, int], max_nodes: int = 32) -> int:\n    # Helper function to count nodes in AST\n    def count_nodes(node):\n        if isinstance(node, tuple):\n            node_type = node[0]\n            if node_type in ('num', 'var'):\n                return 1\n            elif node_type == 'unary':\n                return 1 + count_nodes(node[1])\n            elif node_type in ('add', 'sub', 'mul', 'floordiv'):\n                return 1 + count_nodes(node[1]) + count_nodes(node[2])\n        return 0\n    \n    # Helper function to evaluate AST\n    def eval_node(node):\n        node_type = node[0]\n        \n        if node_type == 'num':\n            return node[1]\n        elif node_type == 'var':\n            var_name = node[1]\n            if var_name not in variables:\n                raise ValueError(f\"Unknown variable: {var_name}\")\n            return variables[var_name]\n        elif node_type == 'unary':\n            return -eval_node(node[1])\n        elif node_type == 'add':\n            return eval_node(node[1]) + eval_node(node[2])\n        elif node_type == 'sub':\n            return eval_node(node[1]) - eval_node(node[2])\n        elif node_type == 'mul':\n            return eval_node(node[1]) * eval_node(node[2])\n        elif node_type == 'floordiv':\n            divisor = eval_node(node[2])\n            if divisor == 0:\n                raise ZeroDivisionError(\"Integer division by zero\")\n            return eval_node(node[1]) // divisor\n    \n    # Simple recursive descent parser\n    def parse_expression(tokens):\n        return parse_add_sub(tokens)\n    \n    def parse_add_sub(tokens):\n        left = parse_mul_div(tokens)\n        \n        while tokens and tokens[0] in ('+', '-'):\n            op = tokens.pop(0)\n            right = parse_mul_div(tokens)\n            if op == '+':\n                left = ('add', left, right)\n            else:\n                left = ('sub', left, right)\n        \n        return left\n    \n    def parse_mul_div(tokens):\n        left = parse_unary(tokens)\n        \n        while tokens and tokens[0] in ('*', '//'):\n            op = tokens.pop(0)\n            right = parse_unary(tokens)\n            if op == '*':\n                left = ('mul', left, right)\n            else:\n                left = ('floordiv', left, right)\n        \n        return left\n    \n    def parse_unary(tokens):\n        if tokens and tokens[0] == '-':\n            tokens.pop(0)\n            expr = parse_unary(tokens)\n            return ('unary', expr)\n        return parse_primary(tokens)\n    \n    def parse_primary(tokens):\n        if not tokens:\n            raise ValueError(\"Unexpected end of expression\")\n        \n        token = tokens.pop(0)\n        \n        if token == '(':\n            expr = parse_expression(tokens)\n            if not tokens or tokens.pop(0) != ')':\n                raise ValueError(\"Missing closing parenthesis\")\n            return expr\n        elif token.isdigit() or (token[0] == '-' and token[1:].isdigit()):\n            return ('num', int(token))\n        elif token.isidentifier():\n            return ('var', token)\n        else:\n            raise ValueError(f\"Unexpected token: {token}\")\n    \n    # Tokenize the expression\n    def tokenize(expr):\n        tokens = []\n        i = 0\n        n = len(expr)\n        \n        while i < n:\n            ch = expr[i]\n            \n            if ch.isspace():\n                i += 1\n            elif ch.isdigit():\n                j = i\n                while j < n and expr[j].isdigit():\n                    j += 1\n                tokens.append(expr[i:j])\n                i = j\n            elif ch.isalpha() or ch == '_':\n                j = i\n                while j < n and (expr[j].isalnum() or expr[j] == '_'):\n                    j += 1\n                tokens.append(expr[i:j])\n                i = j\n            elif ch in '+-*()':\n                # Handle negative numbers\n                if ch == '-' and (not tokens or tokens[-1] in '+-*//('):\n                    j = i + 1\n                    while j < n and expr[j].isdigit():\n                        j += 1\n                    if j > i + 1:  # We found digits after '-'\n                        tokens.append(expr[i:j])\n                        i = j\n                        continue\n                tokens.append(ch)\n                i += 1\n            elif ch == '/':\n                if i + 1 < n and expr[i + 1] == '/':\n                    tokens.append('//')\n                    i += 2\n                else:\n                    raise ValueError(\"Single '/' not allowed, use '//' for integer division\")\n            else:\n                raise ValueError(f\"Invalid character: {ch}\")\n        \n        return tokens\n    \n    try:\n        # Tokenize and parse\n        tokens = tokenize(expression)\n        ast = parse_expression(tokens)\n        \n        # Check for extra tokens\n        if tokens:\n            raise ValueError(f\"Unexpected tokens at end: {tokens}\")\n        \n        # Count nodes and check bounds\n        node_count = count_nodes(ast)\n        if node_count > max_nodes:\n            raise ValueError(f\"Expression exceeds maximum node count: {node_count} > {max_nodes}\")\n        \n        # Evaluate\n        return eval_node(ast)\n        \n    except (ValueError, ZeroDivisionError) as e:\n        # Re-raise with appropriate error type\n        raise e\n```"
}