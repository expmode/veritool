{
  "cache_key": "a5bdb7fc6d6f6d3d6d593a142b0ed81d15223d52e87db12e4037c57efd3cb7cf",
  "code": "def append_only_log(existing: str, new_entry: str, max_bytes: int = 4096) -> str:\n    \"\"\"\n    Append a new log entry without mutating existing bytes.\n    \n    Args:\n        existing: The current log content.\n        new_entry: The new entry to append.\n        max_bytes: Maximum allowed byte length of the output.\n    \n    Returns:\n        The new log content with the entry appended.\n    \n    Raises:\n        ValueError: If the resulting log would exceed max_bytes.\n    \"\"\"\n    # Helper function to calculate byte length\n    def byte_len(s: str) -> int:\n        return len(s.encode('utf-8'))\n    \n    # Calculate the separator needed between existing and new_entry\n    separator = \"\"\n    if existing and not existing.endswith('\\n'):\n        separator = \"\\n\"\n    \n    # Build the potential new log\n    potential_log = existing + separator + new_entry\n    \n    # Check if the new log would exceed max_bytes\n    if byte_len(potential_log) > max_bytes:\n        raise ValueError(\n            f\"Log would exceed {max_bytes} bytes \"\n            f\"(current: {byte_len(existing)}, \"\n            f\"new entry: {byte_len(new_entry)})\"\n        )\n    \n    return potential_log",
  "generated_at": "2026-05-24T22:16:29+00:00",
  "iteration": 1,
  "prompt": "Write Python code for the function `append_only_log`.\nTool name: Append-Only Logger\nSignature: append_only_log(existing: str, new_entry: str, max_bytes: int = 4096) -> str\nTask: Append a new log entry without mutating existing bytes and enforce a hard output-size cap.\nPrimary invariant: The previous log must remain a prefix of the new log; size must never exceed the configured bound.\nConstraints:\n- Preserve the full previous log exactly as a prefix.\n- If the existing log is non-empty and does not end with a newline, insert one before the new entry.\n- Reject outputs whose byte length would exceed max_bytes.\nReturn only valid Python code with the requested function and any helper functions it needs.",
  "provider": "openai-compatible",
  "raw_response": "```python\ndef append_only_log(existing: str, new_entry: str, max_bytes: int = 4096) -> str:\n    \"\"\"\n    Append a new log entry without mutating existing bytes.\n    \n    Args:\n        existing: The current log content.\n        new_entry: The new entry to append.\n        max_bytes: Maximum allowed byte length of the output.\n    \n    Returns:\n        The new log content with the entry appended.\n    \n    Raises:\n        ValueError: If the resulting log would exceed max_bytes.\n    \"\"\"\n    # Helper function to calculate byte length\n    def byte_len(s: str) -> int:\n        return len(s.encode('utf-8'))\n    \n    # Calculate the separator needed between existing and new_entry\n    separator = \"\"\n    if existing and not existing.endswith('\\n'):\n        separator = \"\\n\"\n    \n    # Build the potential new log\n    potential_log = existing + separator + new_entry\n    \n    # Check if the new log would exceed max_bytes\n    if byte_len(potential_log) > max_bytes:\n        raise ValueError(\n            f\"Log would exceed {max_bytes} bytes \"\n            f\"(current: {byte_len(existing)}, \"\n            f\"new entry: {byte_len(new_entry)})\"\n        )\n    \n    return potential_log\n```"
}