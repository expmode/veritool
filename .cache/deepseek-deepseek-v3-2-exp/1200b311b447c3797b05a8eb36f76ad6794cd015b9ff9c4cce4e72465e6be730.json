{
  "cache_key": "1200b311b447c3797b05a8eb36f76ad6794cd015b9ff9c4cce4e72465e6be730",
  "code": "def read_local_context(base_dir: str, requested_path: str, file_map: dict[str, str]) -> str:\n    \"\"\"\n    Normalize a user-requested relative path, reject traversal or non-.txt access,\n    and read the content from the provided in-memory file map.\n    \n    Args:\n        base_dir: The safe base directory path (must end with '/' for consistency)\n        requested_path: User-requested relative path\n        file_map: Dictionary mapping normalized file paths to their content\n        \n    Returns:\n        The content of the requested file as a string\n        \n    Raises:\n        ValueError: If the path is invalid, outside base_dir, or not a .txt file\n        KeyError: If the file is not found in file_map (simulating file not found)\n    \"\"\"\n    \n    # Helper function to normalize paths\n    def normalize_path(base: str, path: str) -> str:\n        \"\"\"Normalize a path relative to base directory.\"\"\"\n        # Ensure base_dir ends with '/'\n        if not base.endswith('/'):\n            base = base + '/'\n        \n        # Reject absolute paths\n        if path.startswith('/'):\n            raise ValueError(\"Absolute paths are not allowed\")\n        \n        # Handle empty path\n        if not path:\n            raise ValueError(\"Empty path is not allowed\")\n        \n        # Split the path into components\n        components = []\n        for component in path.split('/'):\n            if component == '' or component == '.':\n                # Skip empty components and current directory references\n                continue\n            elif component == '..':\n                # Try to go up one directory\n                if components:\n                    components.pop()\n                else:\n                    # Attempted to go above base directory\n                    raise ValueError(\"Path traversal outside base directory\")\n            else:\n                components.append(component)\n        \n        # Join components back together\n        normalized = '/'.join(components)\n        \n        # Check if the normalized path is within base directory\n        # Since we already prevented '..' from escaping, and we're building\n        # from scratch, the path should be safe\n        full_path = base + normalized\n        \n        # Additional safety check: ensure the path starts with base\n        if not full_path.startswith(base):\n            raise ValueError(\"Path is outside the base directory\")\n            \n        return full_path\n    \n    # Normalize the requested path\n    try:\n        normalized_path = normalize_path(base_dir, requested_path)\n    except ValueError as e:\n        raise ValueError(f\"Invalid path: {e}\")\n    \n    # Check if the file is a .txt file\n    if not normalized_path.endswith('.txt'):\n        raise ValueError(\"Only .txt files are allowed\")\n    \n    # Look up the file in the file map\n    if normalized_path not in file_map:\n        raise KeyError(f\"File not found: {normalized_path}\")\n    \n    # Return the content\n    return file_map[normalized_path]",
  "generated_at": "2026-05-24T22:09:34+00:00",
  "iteration": 4,
  "prompt": "Write Python code for the function `read_local_context`.\nTool name: Local Context Reader\nSignature: read_local_context(base_dir: str, requested_path: str, file_map: dict[str, str]) -> str\nTask: Normalize a user-requested relative path, reject traversal or non-.txt access, and read the content from the provided in-memory file map.\nPrimary invariant: A request may only read `.txt` files that remain inside the declared safe base directory after normalization.\nConstraints:\n- Reject absolute paths and any normalized path outside the base directory.\n- Reject files whose normalized path does not end in .txt.\n- Do not touch the real filesystem; use only the provided file_map.\nCounterexamples from previous failed attempts:\n- forbidden_import: Candidates must stay within the provided runtime and cannot import modules.\n  Payload: {\"node_type\": \"Import\"}\n- forbidden_import: Candidates must stay within the provided runtime and cannot import modules.\n  Payload: {\"node_type\": \"Import\"}\n- reader_exception: A valid path should not crash the synthesized reader.\n  Payload: {\"error\": \"Path is outside the base directory\", \"requested_path\": \"ch1.txt\"}\nReturn only valid Python code with the requested function and any helper functions it needs.",
  "provider": "openai-compatible",
  "raw_response": "```python\ndef read_local_context(base_dir: str, requested_path: str, file_map: dict[str, str]) -> str:\n    \"\"\"\n    Normalize a user-requested relative path, reject traversal or non-.txt access,\n    and read the content from the provided in-memory file map.\n    \n    Args:\n        base_dir: The safe base directory path (must end with '/' for consistency)\n        requested_path: User-requested relative path\n        file_map: Dictionary mapping normalized file paths to their content\n        \n    Returns:\n        The content of the requested file as a string\n        \n    Raises:\n        ValueError: If the path is invalid, outside base_dir, or not a .txt file\n        KeyError: If the file is not found in file_map (simulating file not found)\n    \"\"\"\n    \n    # Helper function to normalize paths\n    def normalize_path(base: str, path: str) -> str:\n        \"\"\"Normalize a path relative to base directory.\"\"\"\n        # Ensure base_dir ends with '/'\n        if not base.endswith('/'):\n            base = base + '/'\n        \n        # Reject absolute paths\n        if path.startswith('/'):\n            raise ValueError(\"Absolute paths are not allowed\")\n        \n        # Handle empty path\n        if not path:\n            raise ValueError(\"Empty path is not allowed\")\n        \n        # Split the path into components\n        components = []\n        for component in path.split('/'):\n            if component == '' or component == '.':\n                # Skip empty components and current directory references\n                continue\n            elif component == '..':\n                # Try to go up one directory\n                if components:\n                    components.pop()\n                else:\n                    # Attempted to go above base directory\n                    raise ValueError(\"Path traversal outside base directory\")\n            else:\n                components.append(component)\n        \n        # Join components back together\n        normalized = '/'.join(components)\n        \n        # Check if the normalized path is within base directory\n        # Since we already prevented '..' from escaping, and we're building\n        # from scratch, the path should be safe\n        full_path = base + normalized\n        \n        # Additional safety check: ensure the path starts with base\n        if not full_path.startswith(base):\n            raise ValueError(\"Path is outside the base directory\")\n            \n        return full_path\n    \n    # Normalize the requested path\n    try:\n        normalized_path = normalize_path(base_dir, requested_path)\n    except ValueError as e:\n        raise ValueError(f\"Invalid path: {e}\")\n    \n    # Check if the file is a .txt file\n    if not normalized_path.endswith('.txt'):\n        raise ValueError(\"Only .txt files are allowed\")\n    \n    # Look up the file in the file map\n    if normalized_path not in file_map:\n        raise KeyError(f\"File not found: {normalized_path}\")\n    \n    # Return the content\n    return file_map[normalized_path]\n```"
}