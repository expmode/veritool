{
  "cache_key": "b9bbe4e2584aa45886474b6c9583534e9822cdb4ee199b3c3adccb585a9ccccb",
  "code": "def run_bounded_sql(query: str, rows: list[dict[str, int]]) -> list[dict[str, int]]:\n    def parse_query(query: str):\n        # Basic tokenization and parsing\n        tokens = query.strip().split()\n        if len(tokens) < 4 or tokens[0].upper() != \"SELECT\" or tokens[2].upper() != \"FROM\" or tokens[3] != \"rows\":\n            raise ValueError(\"Invalid SQL syntax\")\n\n        # Extract columns\n        columns = tokens[1].split(',')\n\n        # Check for WHERE clause\n        where_clause = None\n        limit_clause = None\n        if \"WHERE\" in tokens:\n            where_index = tokens.index(\"WHERE\")\n            where_clause = tokens[where_index + 1:where_index + 4]\n            if len(where_clause) != 3:\n                raise ValueError(\"Invalid WHERE clause\")\n            if where_clause[1] not in ['=', '!=', '<', '<=', '>', '>=']:\n                raise ValueError(\"Invalid operator in WHERE clause\")\n            try:\n                where_clause[2] = int(where_clause[2])\n            except ValueError:\n                raise ValueError(\"WHERE clause must compare to an integer\")\n\n        # Check for LIMIT clause\n        if \"LIMIT\" in tokens:\n            limit_index = tokens.index(\"LIMIT\")\n            try:\n                limit_clause = int(tokens[limit_index + 1])\n            except (IndexError, ValueError):\n                raise ValueError(\"Invalid LIMIT clause\")\n\n        return columns, where_clause, limit_clause\n\n    def validate_columns(columns, rows):\n        if not rows:\n            return\n        valid_columns = set(rows[0].keys())\n        for column in columns:\n            if column not in valid_columns:\n                raise ValueError(f\"Unknown column: {column}\")\n\n    def apply_where_clause(row, where_clause):\n        if not where_clause:\n            return True\n        column, op, value = where_clause\n        if column not in row:\n            raise ValueError(f\"Unknown column in WHERE clause: {column}\")\n        if op == '=':\n            return row[column] == value\n        elif op == '!=':\n            return row[column] != value\n        elif op == '<':\n            return row[column] < value\n        elif op == '<=':\n            return row[column] <= value\n        elif op == '>':\n            return row[column] > value\n        elif op == '>=':\n            return row[column] >= value\n        return False\n\n    # Check for forbidden keywords\n    forbidden_keywords = [\"DELETE\", \"UPDATE\", \"INSERT\", \"DROP\", \"ALTER\", \"CREATE\", \"PRAGMA\", \";\"]\n    for keyword in forbidden_keywords:\n        if keyword in query.upper():\n            raise ValueError(f\"Forbidden keyword detected: {keyword}\")\n\n    # Parse the query\n    columns, where_clause, limit_clause = parse_query(query)\n\n    # Validate columns\n    validate_columns(columns, rows)\n\n    # Filter and project rows\n    result = []\n    for row in rows:\n        if apply_where_clause(row, where_clause):\n            projected_row = {col: row[col] for col in columns}\n            result.append(projected_row)\n            if limit_clause is not None and len(result) >= limit_clause:\n                break\n\n    return result",
  "generated_at": "2026-05-24T21:48:39+00:00",
  "iteration": 2,
  "prompt": "Write Python code for the function `run_bounded_sql`.\nTool name: Bounded SQL Querier\nSignature: run_bounded_sql(query: str, rows: list[dict[str, int]]) -> list[dict[str, int]]\nTask: Execute a tiny SELECT-only SQL fragment over an in-memory table of rows.\nPrimary invariant: Only a safe SELECT grammar is allowed; mutating SQL or multi-statement queries must be rejected.\nConstraints:\n- Support only SELECT <columns> FROM rows [WHERE <column> <op> <int>] [LIMIT <int>].\n- Reject DELETE, UPDATE, INSERT, DROP, ALTER, CREATE, PRAGMA, semicolons, and unknown columns.\n- Treat the input `rows` as the only table.\nCounterexamples from previous failed attempts:\n- forbidden_import: Candidates must stay within the provided runtime and cannot import modules.\n  Payload: {\"node_type\": \"Import\"}\nReturn only valid Python code with the requested function and any helper functions it needs.",
  "provider": "openai-compatible",
  "raw_response": "```python\ndef run_bounded_sql(query: str, rows: list[dict[str, int]]) -> list[dict[str, int]]:\n    def parse_query(query: str):\n        # Basic tokenization and parsing\n        tokens = query.strip().split()\n        if len(tokens) < 4 or tokens[0].upper() != \"SELECT\" or tokens[2].upper() != \"FROM\" or tokens[3] != \"rows\":\n            raise ValueError(\"Invalid SQL syntax\")\n\n        # Extract columns\n        columns = tokens[1].split(',')\n\n        # Check for WHERE clause\n        where_clause = None\n        limit_clause = None\n        if \"WHERE\" in tokens:\n            where_index = tokens.index(\"WHERE\")\n            where_clause = tokens[where_index + 1:where_index + 4]\n            if len(where_clause) != 3:\n                raise ValueError(\"Invalid WHERE clause\")\n            if where_clause[1] not in ['=', '!=', '<', '<=', '>', '>=']:\n                raise ValueError(\"Invalid operator in WHERE clause\")\n            try:\n                where_clause[2] = int(where_clause[2])\n            except ValueError:\n                raise ValueError(\"WHERE clause must compare to an integer\")\n\n        # Check for LIMIT clause\n        if \"LIMIT\" in tokens:\n            limit_index = tokens.index(\"LIMIT\")\n            try:\n                limit_clause = int(tokens[limit_index + 1])\n            except (IndexError, ValueError):\n                raise ValueError(\"Invalid LIMIT clause\")\n\n        return columns, where_clause, limit_clause\n\n    def validate_columns(columns, rows):\n        if not rows:\n            return\n        valid_columns = set(rows[0].keys())\n        for column in columns:\n            if column not in valid_columns:\n                raise ValueError(f\"Unknown column: {column}\")\n\n    def apply_where_clause(row, where_clause):\n        if not where_clause:\n            return True\n        column, op, value = where_clause\n        if column not in row:\n            raise ValueError(f\"Unknown column in WHERE clause: {column}\")\n        if op == '=':\n            return row[column] == value\n        elif op == '!=':\n            return row[column] != value\n        elif op == '<':\n            return row[column] < value\n        elif op == '<=':\n            return row[column] <= value\n        elif op == '>':\n            return row[column] > value\n        elif op == '>=':\n            return row[column] >= value\n        return False\n\n    # Check for forbidden keywords\n    forbidden_keywords = [\"DELETE\", \"UPDATE\", \"INSERT\", \"DROP\", \"ALTER\", \"CREATE\", \"PRAGMA\", \";\"]\n    for keyword in forbidden_keywords:\n        if keyword in query.upper():\n            raise ValueError(f\"Forbidden keyword detected: {keyword}\")\n\n    # Parse the query\n    columns, where_clause, limit_clause = parse_query(query)\n\n    # Validate columns\n    validate_columns(columns, rows)\n\n    # Filter and project rows\n    result = []\n    for row in rows:\n        if apply_where_clause(row, where_clause):\n            projected_row = {col: row[col] for col in columns}\n            result.append(projected_row)\n            if limit_clause is not None and len(result) >= limit_clause:\n                break\n\n    return result\n```\n\nThis function parses a simple SQL-like query, validates it, and executes it against an in-memory list of dictionaries. It supports `SELECT`, `WHERE`, and `LIMIT` clauses, while rejecting any unsafe or unsupported SQL operations."
}