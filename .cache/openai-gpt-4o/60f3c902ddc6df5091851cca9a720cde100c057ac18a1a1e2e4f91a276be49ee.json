{
  "cache_key": "60f3c902ddc6df5091851cca9a720cde100c057ac18a1a1e2e4f91a276be49ee",
  "code": "def bounded_evaluate(expression: str, variables: dict[str, int], max_nodes: int = 32) -> int:\n    class NodeCounter:\n        def __init__(self, max_nodes):\n            self.max_nodes = max_nodes\n            self.count = 0\n\n        def increment(self):\n            self.count += 1\n            if self.count > self.max_nodes:\n                raise ValueError(\"Expression exceeds maximum allowed AST nodes\")\n\n    def evaluate(node, counter):\n        counter.increment()\n        if isinstance(node, int):\n            return node\n        elif isinstance(node, str):\n            if node in variables:\n                return variables[node]\n            else:\n                raise ValueError(f\"Unknown variable: {node}\")\n        elif isinstance(node, tuple):\n            op, *args = node\n            if op == 'neg':\n                return -evaluate(args[0], counter)\n            elif op == '+':\n                return evaluate(args[0], counter) + evaluate(args[1], counter)\n            elif op == '-':\n                return evaluate(args[0], counter) - evaluate(args[1], counter)\n            elif op == '*':\n                return evaluate(args[0], counter) * evaluate(args[1], counter)\n            elif op == '//':\n                return evaluate(args[0], counter) // evaluate(args[1], counter)\n            else:\n                raise ValueError(f\"Unsupported operation: {op}\")\n        else:\n            raise ValueError(\"Invalid node type\")\n\n    def parse_expression(expr):\n        tokens = tokenize(expr)\n        return parse_tokens(tokens)\n\n    def tokenize(expr):\n        import re\n        token_spec = [\n            ('NUMBER', r'\\d+'),\n            ('VAR', r'[a-zA-Z_]\\w*'),\n            ('OP', r'[+\\-*/()]'),\n            ('SKIP', r'[ \\t]+'),\n        ]\n        token_regex = '|'.join(f'(?P<{pair[0]}>{pair[1]})' for pair in token_spec)\n        for match in re.finditer(token_regex, expr):\n            kind = match.lastgroup\n            value = match.group()\n            if kind == 'NUMBER':\n                yield int(value)\n            elif kind == 'VAR':\n                yield value\n            elif kind == 'OP':\n                yield value\n            elif kind == 'SKIP':\n                continue\n            else:\n                raise ValueError(f\"Unexpected token: {value}\")\n\n    def parse_tokens(tokens):\n        def parse_primary():\n            token = next(tokens, None)\n            if isinstance(token, int):\n                return token\n            elif isinstance(token, str):\n                if token == '(':\n                    expr = parse_expression()\n                    if next(tokens, None) != ')':\n                        raise ValueError(\"Mismatched parentheses\")\n                    return expr\n                elif token.isidentifier():\n                    return token\n                elif token == '-':\n                    return ('neg', parse_primary())\n                else:\n                    raise ValueError(f\"Unexpected token: {token}\")\n            else:\n                raise ValueError(\"Unexpected end of expression\")\n\n        def parse_term():\n            node = parse_primary()\n            while True:\n                token = next(tokens, None)\n                if token in ('*', '//'):\n                    node = (token, node, parse_primary())\n                else:\n                    if token is not None:\n                        tokens.push(token)\n                    break\n            return node\n\n        def parse_expression():\n            node = parse_term()\n            while True:\n                token = next(tokens, None)\n                if token in ('+', '-'):\n                    node = (token, node, parse_term())\n                else:\n                    if token is not None:\n                        tokens.push(token)\n                    break\n            return node\n\n        tokens = iter(tokens)\n        tokens.push = lambda x: tokens.insert(0, x)\n        return parse_expression()\n\n    counter = NodeCounter(max_nodes)\n    ast = parse_expression(expression)\n    return evaluate(ast, counter)",
  "generated_at": "2026-05-24T21:49:28+00:00",
  "iteration": 4,
  "prompt": "Write Python code for the function `bounded_evaluate`.\nTool name: Compute-Bounded Evaluator\nSignature: bounded_evaluate(expression: str, variables: dict[str, int], max_nodes: int = 32) -> int\nTask: Evaluate a tiny arithmetic expression language with explicit resource bounds.\nPrimary invariant: Only a bounded whitelist of arithmetic AST nodes is allowed, and evaluation must reject unsupported or oversized expressions.\nConstraints:\n- Support integers, variables, parentheses, unary minus, +, -, *, and //.\n- Reject function calls, attributes, exponentiation, unknown variables, and expressions with more than max_nodes AST nodes.\n- Return the same result as the reference interpreter on bounded inputs.\nCounterexamples from previous failed attempts:\n- forbidden_import: Candidates must stay within the provided runtime and cannot import modules.\n  Payload: {\"node_type\": \"Import\"}\n- load_failure: The candidate did not expose the requested function cleanly.\n  Payload: {\"error\": \"__build_class__ not found\", \"function_name\": \"bounded_evaluate\"}\n- forbidden_import: Candidates must stay within the provided runtime and cannot import modules.\n  Payload: {\"node_type\": \"Import\"}\nReturn only valid Python code with the requested function and any helper functions it needs.",
  "provider": "openai-compatible",
  "raw_response": "```python\ndef bounded_evaluate(expression: str, variables: dict[str, int], max_nodes: int = 32) -> int:\n    class NodeCounter:\n        def __init__(self, max_nodes):\n            self.max_nodes = max_nodes\n            self.count = 0\n\n        def increment(self):\n            self.count += 1\n            if self.count > self.max_nodes:\n                raise ValueError(\"Expression exceeds maximum allowed AST nodes\")\n\n    def evaluate(node, counter):\n        counter.increment()\n        if isinstance(node, int):\n            return node\n        elif isinstance(node, str):\n            if node in variables:\n                return variables[node]\n            else:\n                raise ValueError(f\"Unknown variable: {node}\")\n        elif isinstance(node, tuple):\n            op, *args = node\n            if op == 'neg':\n                return -evaluate(args[0], counter)\n            elif op == '+':\n                return evaluate(args[0], counter) + evaluate(args[1], counter)\n            elif op == '-':\n                return evaluate(args[0], counter) - evaluate(args[1], counter)\n            elif op == '*':\n                return evaluate(args[0], counter) * evaluate(args[1], counter)\n            elif op == '//':\n                return evaluate(args[0], counter) // evaluate(args[1], counter)\n            else:\n                raise ValueError(f\"Unsupported operation: {op}\")\n        else:\n            raise ValueError(\"Invalid node type\")\n\n    def parse_expression(expr):\n        tokens = tokenize(expr)\n        return parse_tokens(tokens)\n\n    def tokenize(expr):\n        import re\n        token_spec = [\n            ('NUMBER', r'\\d+'),\n            ('VAR', r'[a-zA-Z_]\\w*'),\n            ('OP', r'[+\\-*/()]'),\n            ('SKIP', r'[ \\t]+'),\n        ]\n        token_regex = '|'.join(f'(?P<{pair[0]}>{pair[1]})' for pair in token_spec)\n        for match in re.finditer(token_regex, expr):\n            kind = match.lastgroup\n            value = match.group()\n            if kind == 'NUMBER':\n                yield int(value)\n            elif kind == 'VAR':\n                yield value\n            elif kind == 'OP':\n                yield value\n            elif kind == 'SKIP':\n                continue\n            else:\n                raise ValueError(f\"Unexpected token: {value}\")\n\n    def parse_tokens(tokens):\n        def parse_primary():\n            token = next(tokens, None)\n            if isinstance(token, int):\n                return token\n            elif isinstance(token, str):\n                if token == '(':\n                    expr = parse_expression()\n                    if next(tokens, None) != ')':\n                        raise ValueError(\"Mismatched parentheses\")\n                    return expr\n                elif token.isidentifier():\n                    return token\n                elif token == '-':\n                    return ('neg', parse_primary())\n                else:\n                    raise ValueError(f\"Unexpected token: {token}\")\n            else:\n                raise ValueError(\"Unexpected end of expression\")\n\n        def parse_term():\n            node = parse_primary()\n            while True:\n                token = next(tokens, None)\n                if token in ('*', '//'):\n                    node = (token, node, parse_primary())\n                else:\n                    if token is not None:\n                        tokens.push(token)\n                    break\n            return node\n\n        def parse_expression():\n            node = parse_term()\n            while True:\n                token = next(tokens, None)\n                if token in ('+', '-'):\n                    node = (token, node, parse_term())\n                else:\n                    if token is not None:\n                        tokens.push(token)\n                    break\n            return node\n\n        tokens = iter(tokens)\n        tokens.push = lambda x: tokens.insert(0, x)\n        return parse_expression()\n\n    counter = NodeCounter(max_nodes)\n    ast = parse_expression(expression)\n    return evaluate(ast, counter)\n```"
}