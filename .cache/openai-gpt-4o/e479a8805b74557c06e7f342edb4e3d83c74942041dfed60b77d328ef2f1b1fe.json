{
  "cache_key": "e479a8805b74557c06e7f342edb4e3d83c74942041dfed60b77d328ef2f1b1fe",
  "code": "def run_bounded_sql(query: str, rows: list[dict[str, int]]) -> list[dict[str, int]]:\n    def parse_query(query: str):\n        # Basic checks for forbidden keywords and semicolons\n        forbidden_keywords = [\"DELETE\", \"UPDATE\", \"INSERT\", \"DROP\", \"ALTER\", \"CREATE\", \"PRAGMA\", \";\"]\n        for keyword in forbidden_keywords:\n            if keyword in query.upper():\n                raise ValueError(f\"Forbidden keyword detected: {keyword}\")\n\n        # Split query into parts\n        parts = query.strip().split()\n        if len(parts) < 4 or parts[0].upper() != \"SELECT\" or parts[2].upper() != \"FROM\":\n            raise ValueError(\"Invalid query format\")\n\n        # Extract columns\n        columns = parts[1].split(',')\n        if columns == ['*']:\n            columns = list(rows[0].keys()) if rows else []\n\n        # Check for unknown columns\n        for column in columns:\n            if column not in rows[0]:\n                raise ValueError(f\"Unknown column: {column}\")\n\n        # Extract WHERE clause if present\n        where_clause = None\n        limit = None\n        if \"WHERE\" in parts:\n            where_index = parts.index(\"WHERE\")\n            where_clause = parts[where_index + 1:where_index + 4]\n            if len(where_clause) != 3:\n                raise ValueError(\"Invalid WHERE clause format\")\n            if where_clause[0] not in rows[0]:\n                raise ValueError(f\"Unknown column in WHERE clause: {where_clause[0]}\")\n            if where_clause[1] not in [\"=\", \">\", \"<\", \">=\", \"<=\"]:\n                raise ValueError(f\"Invalid operator in WHERE clause: {where_clause[1]}\")\n            try:\n                where_clause[2] = int(where_clause[2])\n            except ValueError:\n                raise ValueError(\"WHERE clause value must be an integer\")\n\n        # Extract LIMIT clause if present\n        if \"LIMIT\" in parts:\n            limit_index = parts.index(\"LIMIT\")\n            try:\n                limit = int(parts[limit_index + 1])\n            except (IndexError, ValueError):\n                raise ValueError(\"Invalid LIMIT clause format\")\n\n        return columns, where_clause, limit\n\n    def apply_where_clause(row, where_clause):\n        if not where_clause:\n            return True\n        column, operator, value = where_clause\n        if operator == \"=\":\n            return row[column] == value\n        elif operator == \">\":\n            return row[column] > value\n        elif operator == \"<\":\n            return row[column] < value\n        elif operator == \">=\":\n            return row[column] >= value\n        elif operator == \"<=\":\n            return row[column] <= value\n        return False\n\n    columns, where_clause, limit = parse_query(query)\n    filtered_rows = [row for row in rows if apply_where_clause(row, where_clause)]\n    selected_rows = [{col: row[col] for col in columns} for row in filtered_rows]\n\n    if limit is not None:\n        return selected_rows[:limit]\n    return selected_rows",
  "generated_at": "2026-05-24T21:48:44+00:00",
  "iteration": 3,
  "prompt": "Write Python code for the function `run_bounded_sql`.\nTool name: Bounded SQL Querier\nSignature: run_bounded_sql(query: str, rows: list[dict[str, int]]) -> list[dict[str, int]]\nTask: Execute a tiny SELECT-only SQL fragment over an in-memory table of rows.\nPrimary invariant: Only a safe SELECT grammar is allowed; mutating SQL or multi-statement queries must be rejected.\nConstraints:\n- Support only SELECT <columns> FROM rows [WHERE <column> <op> <int>] [LIMIT <int>].\n- Reject DELETE, UPDATE, INSERT, DROP, ALTER, CREATE, PRAGMA, semicolons, and unknown columns.\n- Treat the input `rows` as the only table.\nCounterexamples from previous failed attempts:\n- forbidden_import: Candidates must stay within the provided runtime and cannot import modules.\n  Payload: {\"node_type\": \"Import\"}\n- sql_exception: A safe query should execute cleanly in the bounded runner.\n  Payload: {\"error\": \"Unknown column: *\", \"query\": \"SELECT * FROM rows\"}\nReturn only valid Python code with the requested function and any helper functions it needs.",
  "provider": "openai-compatible",
  "raw_response": "```python\ndef run_bounded_sql(query: str, rows: list[dict[str, int]]) -> list[dict[str, int]]:\n    def parse_query(query: str):\n        # Basic checks for forbidden keywords and semicolons\n        forbidden_keywords = [\"DELETE\", \"UPDATE\", \"INSERT\", \"DROP\", \"ALTER\", \"CREATE\", \"PRAGMA\", \";\"]\n        for keyword in forbidden_keywords:\n            if keyword in query.upper():\n                raise ValueError(f\"Forbidden keyword detected: {keyword}\")\n\n        # Split query into parts\n        parts = query.strip().split()\n        if len(parts) < 4 or parts[0].upper() != \"SELECT\" or parts[2].upper() != \"FROM\":\n            raise ValueError(\"Invalid query format\")\n\n        # Extract columns\n        columns = parts[1].split(',')\n        if columns == ['*']:\n            columns = list(rows[0].keys()) if rows else []\n\n        # Check for unknown columns\n        for column in columns:\n            if column not in rows[0]:\n                raise ValueError(f\"Unknown column: {column}\")\n\n        # Extract WHERE clause if present\n        where_clause = None\n        limit = None\n        if \"WHERE\" in parts:\n            where_index = parts.index(\"WHERE\")\n            where_clause = parts[where_index + 1:where_index + 4]\n            if len(where_clause) != 3:\n                raise ValueError(\"Invalid WHERE clause format\")\n            if where_clause[0] not in rows[0]:\n                raise ValueError(f\"Unknown column in WHERE clause: {where_clause[0]}\")\n            if where_clause[1] not in [\"=\", \">\", \"<\", \">=\", \"<=\"]:\n                raise ValueError(f\"Invalid operator in WHERE clause: {where_clause[1]}\")\n            try:\n                where_clause[2] = int(where_clause[2])\n            except ValueError:\n                raise ValueError(\"WHERE clause value must be an integer\")\n\n        # Extract LIMIT clause if present\n        if \"LIMIT\" in parts:\n            limit_index = parts.index(\"LIMIT\")\n            try:\n                limit = int(parts[limit_index + 1])\n            except (IndexError, ValueError):\n                raise ValueError(\"Invalid LIMIT clause format\")\n\n        return columns, where_clause, limit\n\n    def apply_where_clause(row, where_clause):\n        if not where_clause:\n            return True\n        column, operator, value = where_clause\n        if operator == \"=\":\n            return row[column] == value\n        elif operator == \">\":\n            return row[column] > value\n        elif operator == \"<\":\n            return row[column] < value\n        elif operator == \">=\":\n            return row[column] >= value\n        elif operator == \"<=\":\n            return row[column] <= value\n        return False\n\n    columns, where_clause, limit = parse_query(query)\n    filtered_rows = [row for row in rows if apply_where_clause(row, where_clause)]\n    selected_rows = [{col: row[col] for col in columns} for row in filtered_rows]\n\n    if limit is not None:\n        return selected_rows[:limit]\n    return selected_rows\n```"
}