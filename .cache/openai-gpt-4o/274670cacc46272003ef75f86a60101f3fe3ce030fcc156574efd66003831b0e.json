{
  "cache_key": "274670cacc46272003ef75f86a60101f3fe3ce030fcc156574efd66003831b0e",
  "code": "def append_only_log(existing: str, new_entry: str, max_bytes: int = 4096) -> str:\n    # Check for NUL bytes in the new entry\n    if '\\x00' in new_entry:\n        raise ValueError(\"New entry contains NUL bytes, which are not allowed.\")\n    \n    # Prepare the separator if needed\n    separator = '\\n' if existing and not existing.endswith('\\n') else ''\n    \n    # Construct the new log\n    new_log = existing + separator + new_entry\n    \n    # Check if the new log exceeds the maximum allowed bytes\n    if len(new_log.encode('utf-8')) > max_bytes:\n        raise ValueError(\"Appending the new entry would exceed the maximum allowed size.\")\n    \n    return new_log",
  "generated_at": "2026-05-24T21:49:04+00:00",
  "iteration": 3,
  "prompt": "Write Python code for the function `append_only_log`.\nTool name: Append-Only Logger\nSignature: append_only_log(existing: str, new_entry: str, max_bytes: int = 4096) -> str\nTask: Append a new log entry without mutating existing bytes and enforce a hard output-size cap.\nPrimary invariant: The previous log must remain a prefix of the new log; size must never exceed the configured bound.\nConstraints:\n- Preserve the full previous log exactly as a prefix.\n- If the existing log is non-empty and does not end with a newline, insert one before the new entry.\n- Reject outputs whose byte length would exceed max_bytes.\nCounterexamples from previous failed attempts:\n- invalid_append_accepted: Oversized writes and NUL bytes should be rejected.\n  Payload: {\"existing\": \"\", \"max_bytes\": 64, \"new_entry\": \"abc\\u0000def\"}\n- invalid_append_accepted: Oversized writes and NUL bytes should be rejected.\n  Payload: {\"existing\": \"seed\", \"max_bytes\": 16, \"new_entry\": \"xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx\"}\nReturn only valid Python code with the requested function and any helper functions it needs.",
  "provider": "openai-compatible",
  "raw_response": "```python\ndef append_only_log(existing: str, new_entry: str, max_bytes: int = 4096) -> str:\n    # Check for NUL bytes in the new entry\n    if '\\x00' in new_entry:\n        raise ValueError(\"New entry contains NUL bytes, which are not allowed.\")\n    \n    # Prepare the separator if needed\n    separator = '\\n' if existing and not existing.endswith('\\n') else ''\n    \n    # Construct the new log\n    new_log = existing + separator + new_entry\n    \n    # Check if the new log exceeds the maximum allowed bytes\n    if len(new_log.encode('utf-8')) > max_bytes:\n        raise ValueError(\"Appending the new entry would exceed the maximum allowed size.\")\n    \n    return new_log\n```\n\nThis function ensures that:\n- The existing log remains unchanged as a prefix.\n- A newline is added between the existing log and the new entry if the existing log doesn't already end with one.\n- NUL bytes in the new entry are rejected.\n- The total size of the log does not exceed the specified `max_bytes` limit."
}