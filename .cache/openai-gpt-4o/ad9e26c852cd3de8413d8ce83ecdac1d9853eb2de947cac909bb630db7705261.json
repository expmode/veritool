{
  "cache_key": "ad9e26c852cd3de8413d8ce83ecdac1d9853eb2de947cac909bb630db7705261",
  "code": "class Node:\n    def __init__(self, node_type, value=None, left=None, right=None):\n        self.node_type = node_type\n        self.value = value\n        self.left = left\n        self.right = right\n\ndef parse_expression(expression, variables):\n    tokens = tokenize(expression)\n    return parse_tokens(tokens, variables)\n\ndef tokenize(expression):\n    tokens = []\n    i = 0\n    while i < len(expression):\n        char = expression[i]\n        if char.isspace():\n            i += 1\n            continue\n        elif char.isdigit():\n            num = char\n            i += 1\n            while i < len(expression) and expression[i].isdigit():\n                num += expression[i]\n                i += 1\n            tokens.append(('INT', int(num)))\n        elif char.isalpha():\n            var = char\n            i += 1\n            while i < len(expression) and expression[i].isalpha():\n                var += expression[i]\n                i += 1\n            tokens.append(('VAR', var))\n        elif char in '+-*/()':\n            tokens.append(('OP', char))\n            i += 1\n        else:\n            raise ValueError(f\"Invalid character in expression: {char}\")\n    return tokens\n\ndef parse_tokens(tokens, variables):\n    def parse_primary():\n        token = tokens.pop(0)\n        if token[0] == 'INT':\n            return Node('INT', token[1])\n        elif token[0] == 'VAR':\n            if token[1] in variables:\n                return Node('INT', variables[token[1]])\n            else:\n                raise ValueError(f\"Unknown variable: {token[1]}\")\n        elif token[0] == 'OP' and token[1] == '(':\n            node = parse_expression()\n            if tokens.pop(0)[1] != ')':\n                raise ValueError(\"Mismatched parentheses\")\n            return node\n        elif token[0] == 'OP' and token[1] == '-':\n            return Node('NEG', parse_primary())\n        else:\n            raise ValueError(\"Invalid primary expression\")\n\n    def parse_term():\n        node = parse_primary()\n        while tokens and tokens[0][0] == 'OP' and tokens[0][1] in '*/':\n            op = tokens.pop(0)[1]\n            right = parse_primary()\n            node = Node('BINOP', op, node, right)\n        return node\n\n    def parse_expression():\n        node = parse_term()\n        while tokens and tokens[0][0] == 'OP' and tokens[0][1] in '+-':\n            op = tokens.pop(0)[1]\n            right = parse_term()\n            node = Node('BINOP', op, node, right)\n        return node\n\n    return parse_expression()\n\ndef count_nodes(node):\n    if node is None:\n        return 0\n    count = 1\n    if node.left:\n        count += count_nodes(node.left)\n    if node.right:\n        count += count_nodes(node.right)\n    return count\n\ndef evaluate(node):\n    if node.node_type == 'INT':\n        return node.value\n    elif node.node_type == 'NEG':\n        return -evaluate(node.left)\n    elif node.node_type == 'BINOP':\n        left_val = evaluate(node.left)\n        right_val = evaluate(node.right)\n        if node.value == '+':\n            return left_val + right_val\n        elif node.value == '-':\n            return left_val - right_val\n        elif node.value == '*':\n            return left_val * right_val\n        elif node.value == '//':\n            if right_val == 0:\n                raise ZeroDivisionError(\"Division by zero\")\n            return left_val // right_val\n    raise ValueError(\"Invalid node type\")\n\ndef bounded_evaluate(expression: str, variables: dict[str, int], max_nodes: int = 32) -> int:\n    ast = parse_expression(expression, variables)\n    node_count = count_nodes(ast)\n    if node_count > max_nodes:\n        raise ValueError(\"Expression exceeds maximum allowed nodes\")\n    return evaluate(ast)",
  "generated_at": "2026-05-24T21:49:19+00:00",
  "iteration": 2,
  "prompt": "Write Python code for the function `bounded_evaluate`.\nTool name: Compute-Bounded Evaluator\nSignature: bounded_evaluate(expression: str, variables: dict[str, int], max_nodes: int = 32) -> int\nTask: Evaluate a tiny arithmetic expression language with explicit resource bounds.\nPrimary invariant: Only a bounded whitelist of arithmetic AST nodes is allowed, and evaluation must reject unsupported or oversized expressions.\nConstraints:\n- Support integers, variables, parentheses, unary minus, +, -, *, and //.\n- Reject function calls, attributes, exponentiation, unknown variables, and expressions with more than max_nodes AST nodes.\n- Return the same result as the reference interpreter on bounded inputs.\nCounterexamples from previous failed attempts:\n- forbidden_import: Candidates must stay within the provided runtime and cannot import modules.\n  Payload: {\"node_type\": \"Import\"}\nReturn only valid Python code with the requested function and any helper functions it needs.",
  "provider": "openai-compatible",
  "raw_response": "```python\nclass Node:\n    def __init__(self, node_type, value=None, left=None, right=None):\n        self.node_type = node_type\n        self.value = value\n        self.left = left\n        self.right = right\n\ndef parse_expression(expression, variables):\n    tokens = tokenize(expression)\n    return parse_tokens(tokens, variables)\n\ndef tokenize(expression):\n    tokens = []\n    i = 0\n    while i < len(expression):\n        char = expression[i]\n        if char.isspace():\n            i += 1\n            continue\n        elif char.isdigit():\n            num = char\n            i += 1\n            while i < len(expression) and expression[i].isdigit():\n                num += expression[i]\n                i += 1\n            tokens.append(('INT', int(num)))\n        elif char.isalpha():\n            var = char\n            i += 1\n            while i < len(expression) and expression[i].isalpha():\n                var += expression[i]\n                i += 1\n            tokens.append(('VAR', var))\n        elif char in '+-*/()':\n            tokens.append(('OP', char))\n            i += 1\n        else:\n            raise ValueError(f\"Invalid character in expression: {char}\")\n    return tokens\n\ndef parse_tokens(tokens, variables):\n    def parse_primary():\n        token = tokens.pop(0)\n        if token[0] == 'INT':\n            return Node('INT', token[1])\n        elif token[0] == 'VAR':\n            if token[1] in variables:\n                return Node('INT', variables[token[1]])\n            else:\n                raise ValueError(f\"Unknown variable: {token[1]}\")\n        elif token[0] == 'OP' and token[1] == '(':\n            node = parse_expression()\n            if tokens.pop(0)[1] != ')':\n                raise ValueError(\"Mismatched parentheses\")\n            return node\n        elif token[0] == 'OP' and token[1] == '-':\n            return Node('NEG', parse_primary())\n        else:\n            raise ValueError(\"Invalid primary expression\")\n\n    def parse_term():\n        node = parse_primary()\n        while tokens and tokens[0][0] == 'OP' and tokens[0][1] in '*/':\n            op = tokens.pop(0)[1]\n            right = parse_primary()\n            node = Node('BINOP', op, node, right)\n        return node\n\n    def parse_expression():\n        node = parse_term()\n        while tokens and tokens[0][0] == 'OP' and tokens[0][1] in '+-':\n            op = tokens.pop(0)[1]\n            right = parse_term()\n            node = Node('BINOP', op, node, right)\n        return node\n\n    return parse_expression()\n\ndef count_nodes(node):\n    if node is None:\n        return 0\n    count = 1\n    if node.left:\n        count += count_nodes(node.left)\n    if node.right:\n        count += count_nodes(node.right)\n    return count\n\ndef evaluate(node):\n    if node.node_type == 'INT':\n        return node.value\n    elif node.node_type == 'NEG':\n        return -evaluate(node.left)\n    elif node.node_type == 'BINOP':\n        left_val = evaluate(node.left)\n        right_val = evaluate(node.right)\n        if node.value == '+':\n            return left_val + right_val\n        elif node.value == '-':\n            return left_val - right_val\n        elif node.value == '*':\n            return left_val * right_val\n        elif node.value == '//':\n            if right_val == 0:\n                raise ZeroDivisionError(\"Division by zero\")\n            return left_val // right_val\n    raise ValueError(\"Invalid node type\")\n\ndef bounded_evaluate(expression: str, variables: dict[str, int], max_nodes: int = 32) -> int:\n    ast = parse_expression(expression, variables)\n    node_count = count_nodes(ast)\n    if node_count > max_nodes:\n        raise ValueError(\"Expression exceeds maximum allowed nodes\")\n    return evaluate(ast)\n```"
}